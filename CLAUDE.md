# CLAUDE.md

Claude Code reads this file; every other agent tool reads `AGENTS.md`. To keep one source of truth,
this file only imports that one — the line below pulls its full contents into context.

@AGENTS.md

Do not add rules here. They belong in `AGENTS.md`, or in a skill under `skills/`.
