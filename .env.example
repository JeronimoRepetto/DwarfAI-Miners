# AgentName configuration.
# Copy to `.env` and adjust. All values are optional - defaults shown below.

# How often the provider scanner polls for agent activity (milliseconds, integer >= 1).
POLL_INTERVAL_MS=2000

# How long an agent counts as alive after its last observed activity (seconds, integer >= 1).
LIVENESS_WINDOW_S=90
